A reusable, raw React components library (not transpiled).
Import elements directly, e.g., <code>import { confirm } from "react-materials/prompts"</code>.

When using this repository as a dependency, reference a specific commit instead of a branch.
It’s not intended for public use. Updates may break backward compatibility.